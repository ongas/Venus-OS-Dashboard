{
  "name": "venus-os-dashboard-ongas",
  "version": "0.1.1",
  "description": "Home Assistant Lovelace custom card for Venus OS Dashboard (ongas)",
  "devDependencies": {
    "eslint": "^8.0.0",
    "puppeteer": "^24.40.0"
  },
  "scripts": {
    "lint": "eslint dist/*.js",
    "test": "node test/gauge-exceeded.test.js && node test/editor-tabs.test.js",
    "build": "npm run lint && for f in dist/*.js; do gzip -c \"$f\" > \"$f\".gz; done"
  },
  "keywords": [
    "home-assistant",
    "lovelace",
    "custom-card",
    "venus-os"
  ],
  "author": "ongas",
  "license": "MIT"
}
